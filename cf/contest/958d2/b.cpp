#include <bits/stdc++.h>

using namespace std;
void solve() {
    int N; cin >> N;
    string s; cin >> s;
    //set to 0
    //when is it impossible?
    //the goal is to combine as many 0s as we can
    //wait we can always remove a 0 and a 1
    //uh thats not ideal though
    //figure out which segment to use as majority
    //then figure out which segment to swap
    //if we delete 1 0 and 1 1 together is that even suboptimal
    //
    for (int i = 0; i < N; i++) {

    }
}
int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int T; cin >> T; while(T) { T--; solve(); }
}