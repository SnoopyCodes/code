#include <bits/stdc++.h>

using std::vector, std::array, std::min, std::max, std::cin, std::cout, std::swap;

void solve() {
    //this is probably not too bad with ds
    //we only need to know the edges outside of each individual path
    //find out how many pass through each edge
    //initially it is w * N
    //
}

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int T; cin >> T; while(T--) { solve(); }
}