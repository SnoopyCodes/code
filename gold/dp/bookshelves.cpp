#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //the only viable bits are those that exist in more than k numbers
    //we must always try to maximize the largest bit
    //try the 50th bit
    //uhhhhhhhhhhhhhhh
    //?
    //we need to try for maximizing the first bit
    //cant we like brute force or smth
    //like this is not hard i think
    //whatever we come up with will run in time
    //probably memlim is touher
    //
}