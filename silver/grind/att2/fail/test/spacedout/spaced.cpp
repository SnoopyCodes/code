#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    //honestly just brute force it and give up
    //this is hard
    //first look at 1d problem
    //every group of 2 must have 1
    //this feels like a dp problem
    //what if we try placing in the most valuable spots?
    //that yields a painful number
    //
}