#include <bits/stdc++.h>
#define ll long long
using namespace std;
const ll MOD = 1e9+7;
ll exp(ll a, ll b) {
    ll res = a;
    while (a > 0) {
        
    }
}
int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    ll a, b; cin >> a >> b;
    cout << exp(a % MOD, b % MOD);
    //calculate a ^ b mod 1e9+7
}