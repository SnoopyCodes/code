#include <bits/stdc++.h>
#define vec vector
using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    vec<int> v;
    vector<vector<vector<int>>> ggg(2, vector<vector<int>> (4, vector<int> (3)));
    vec<vec<vec<int>>> ggg(10, vec<vec<int>> (100, vec<int>(2)));
}