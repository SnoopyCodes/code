#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    string s; cin >> s;
    int q; cin >> q;
    //anagrams are when both words have the same character count for each character
    //ah yes making 26 prefix sum arrays
    //how do you get an irreducible anagram
    for (int Q = 0; Q < q; Q++) {
        
    }
}