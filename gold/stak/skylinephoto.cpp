#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //we are so back
    int N; cin >> N;
    vector<int> H(N);
    vector<int> B(N);
    for (int i = 0; i < N; i++) {
        cin >> H[i];
    }
    for (int i = 0; i < N; i++) {
        cin >> B[i];
    }
    
}