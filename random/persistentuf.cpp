#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //what?
    int N, Q; cin >> N >> Q;
    //what if we draw an edge from graph to graph lmfao
    //stating that this is the edge that is drawn
    
}