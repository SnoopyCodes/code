#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //L substring within larger K substring
    //sorting that is crazy
    //can we calculate the length x starting at some position the number of lex mins
    //it will count for?
    //
}