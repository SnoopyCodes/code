#include <bits/stdc++.h>

using namespace std;

#define long long long
const int BIG = 1e9 + 1;
const long INF = 4e18 + 1000;
template<int z> using ints = array<int, z>;
template<int z> using longs = array<long, z>;
template<class T> using vt = vector<T>;
template<class T> using vv = vt<vt<T>>;
#define add push_back
#define poll pop_back
template<class T> struct qq : deque<T> { using deque<T>::deque; void pop() { this->pop_front(); } };

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    qq<int> q;
    q.add(2);
    q.pop();
    vt<int> st;
}