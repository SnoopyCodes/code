#include <bits/stdc++.h>

using namespace std;
const int MOD = 1e9 + 7;
int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int K; cin >> K;
    string s; cin >> s;
    
    //yeesh
    //ok so we should actually try to do math
    //26..
    //am i stupid
    //isnt this like just stars and bars.
    //its not
    //consider "a"
    //aa == aa
    //when do we get the same strings?
    //we want to count the number of valid things we can get from
}