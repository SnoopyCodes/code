#include <bits/stdc++.h>
#define ll long long
using namespace std;
vector<int> salt;
int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int N, M; cin >> N >> M;
    salt.resize(N);
    for (int i = 0; i < N; i++) {
        cin >> salt[i];
    }
    //brain damage bruh
    for (int i = 0; i < N; i++) {
        
    }
}