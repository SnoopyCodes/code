#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int N, Q; cin >> N >> Q;
    //is this done using like a difference array?
    //okay uh
    //hm
    //what do we store at each of the nodes?
    //the maximumm and minimum prefix sum up to that point?
    //because that will definitely give us the maximum
    //how does modification work
    //okay. fine.
    //
}