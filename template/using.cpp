#include <bits/stdc++.h>

// using std::vector, std::array, std::cin, std::cout, std::min, std::max, std::swap;
//annoying things
//i could get by without the last row
//i just wanna use count and size as actual variables :(
//solutions
using namespace std;

vector<int> count;
int main() {
    cin.tie(0) -> sync_with_stdio(0);
    
}