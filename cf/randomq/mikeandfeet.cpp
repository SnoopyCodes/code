#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N; cin >> N;
    vector<int> v;
    for (int i = 0; i < N; i++) {
        cin >> v[i];
    }
    vector<int> unavoidable(N);
    //we can use a stack ?
    //we can figure out for which ones in which size they will appear unavoidable
    for (int i = 0; i < N; i++) {
        //
    }
}