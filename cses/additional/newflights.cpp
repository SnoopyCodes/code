#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //find sccs, and figure out which ones can reach which
    //by sccs, we reduce it to: for a DAG, find min edges to add
    //so all nodes can reach all others
    //this is very hard
    //dp[i] = min to connect all components of 
}