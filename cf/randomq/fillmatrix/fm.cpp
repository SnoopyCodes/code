#include <bits/stdc++.h>

using namespace std;
void solve() {
    int N, M; cin >> N >> M;
    //so we have to have a mex value of 0
    //what does this mean
    //so say we have more elements than is necessary for this, ie M >= N
    //then we need to prove that the answer is just M
    //otherwise, N?
}
int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int T; cin >> T; while (T) { T--; solve(); }
}