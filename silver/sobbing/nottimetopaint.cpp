#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //record for every color the segments used
    //we can use a segment tree skull to find the maximum within a range
    //anyways then we just do bounds checking
    //if we entirely contain a range, decrease
    //if we are contained within a range, increase
    //how would we find the segments used?
    //
}