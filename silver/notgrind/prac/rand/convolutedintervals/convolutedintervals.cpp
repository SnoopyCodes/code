#include <bits/stdc++.h>

using namespace std;
vector<pair<int, int>> intervals;
int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int N, M; cin >> N >> M;
    for (int i = 0; i < N; i++) {
        cin >> intervals[i].first >> intervals[i].second;
    }
    //okay now what
    //give up
    //help hes coming over
    //what do i do
    //oh hes leaving
    //and he took my shoes
    //its actually over
    //okay store for every N an array M
    //and then for each N
    //assuming each array is prefix summed
    //setup new indexes where it makes contributions
    // 
    for (int i = 0; i < N; i++) {
        //so in the case of our 2nd in the sample input
        //we need 2 arrays for each N: one for the base ones
        //one for the non base ones
        //for the non base if we come across one do (original + (base+1))
        //add 1 to the base
    }
}