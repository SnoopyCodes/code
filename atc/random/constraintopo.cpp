#include <bits/stdc++.h>

using namespace std;

#define rsz resize
#define emp emplace
#define emb emplace_back
#define pob pop_back

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N, M; cin >> N >> M;
    vector<vector<int>> G(N);
}