#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N, M; cin >> N >> M;
    //so basically
    //do we run a toposort?
    //we cant
    //do a first dfs pass
    //so we can combine children at first
    //for all directed edges
    //all the destinations must then have the same color children
    //for the reverse adj
    //all the destinations have the same color
    //insane gaempaly
    //give the dsu value as a negative number
    //for whatever vertice its going to
    
}