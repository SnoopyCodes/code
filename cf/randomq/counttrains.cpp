#include <bits/stdc++.h>

using namespace std;

const int P = 1e9 + 7;
template<class T> using vt = vector<T>;
template<int z> using ii = array<int, z>;

void solve() {
    int N, M; cin >> N >> M;
    /*
    the problem can be brute forced with a lazy propagation segment tree.
    instead, it can be tediously done using binary search.
    we have maxspeed for each, and inside a set we store current groups { start index, end index, speed }
    
    */
}

signed main() {
    cin.tie(0) -> sync_with_stdio(0);
    int T; cin >> T; while (T --> 0) solve();
}