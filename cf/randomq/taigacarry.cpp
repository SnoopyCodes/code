#include <bits/stdc++.h>

using namespace std;

#define rep(i, a, b) for (auto i = a; i < (b); i++) //4 times to be worth
#define vi vector<int> //3 times
template<int z> using ii = array<int, z>; //~5 times

void solve() {
    int N, K; cin >> N >> K;
    //#define rep(i, a, b) for (int i = a; i < (b); i++)
    //for (int i = 0; i < N; i++)
    if (K > 31) {
        
    }
}

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int T; cin >> T; while (T --> 0) solve(); 
}