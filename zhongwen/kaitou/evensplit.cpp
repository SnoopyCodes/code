#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N; cin >> N;
    //first nontrivial knapsack?
    vector<int> A(N);
    for (int i = 0; i < N; i++) {
        cin >> A[i];
    }
    //N <= 500
    //sum of A <= 1e5
    //500 * 1e5 somehow
    for (int i = 0; i < N; i++) {
        
    }
}