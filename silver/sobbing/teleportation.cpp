#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //freopen("teleport.in", "r", stdin);
    //freopen("teleport.out", "w", stdout);
    int N; cin >> N;
    //is this ternary search :sob:
    //
}