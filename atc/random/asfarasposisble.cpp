#include <bits/stdc++.h>

using namespace std;
vector<vector<int>> tree;
int main() {
    cin.tie(0) -> sync_with_stdio(0);
    //jesus christ what on earth
    //determine for every single node the distance from root i guess
    //then we know distance for every single node
    //euler tour
    //every single node on the path from the root to our target node's distance is set to 0
    
    //basically find the maximum distance from each node to the root
    //every single time
    //the problem is how to update it correctly
    //
    int N; cin >> N;
    tree.resize(N);

}