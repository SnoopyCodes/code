#include <bits/stdc++.h>

using namespace std;
vector<int> numbers;
int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int n, x;
    numbers.resize(n);
    for (int i = 0; i < n; i++) {
        
    }
}