#include <bits/stdc++.h>
#define rsz resize
using namespace std;

vector<vector<int>> tree;
vector<int> depth, parent, sz;


int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N, Q; cin >> N >> Q;
    tree.resize(N);
}