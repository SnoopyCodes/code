#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N, M; cin >> N >> M;
    //is this not just look at smallest a-b?
    //it is, but we need to do it for every c
    
}