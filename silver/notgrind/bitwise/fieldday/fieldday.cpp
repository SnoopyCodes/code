#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int N; cin >> N;
    for (int i = 0; i < N; i++) {
        
    }
    //we can iterate through each one and find the min edit distance to check how far away this is
    //
}