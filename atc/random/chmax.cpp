#include <bits/stdc++.h>

using namespace std;

const int P = 1e9 + 7;
template<class T> using vt = vector<T>;
template<int z> using ii = array<int, z>;

signed main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N; cin >> N;
    vt<int> P(N);
    for (int &u : P) {
        cin >> u;
    }
    int x = 0, y = 0, c = 0;
    //ok now to be not stupid. 
    cout << c << endl;
}