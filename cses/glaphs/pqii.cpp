#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int N, Q; cin >> N >> Q;
    int max_jumps = __lg(N - 1) + 1;
    vector<vector<int>> jumps(max_jumps, vector<int>(N));
    for (int i = 0; i < N; i++) {
        
    }
    //
}